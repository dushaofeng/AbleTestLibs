<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.able.libs">

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".KotlinAndBezierTestActivity"/>
        <activity android:name=".AlgorithmActivity"/>
        <activity android:name=".AATestActivity_"/>
        <activity
            android:name=".SenseyTestActivity_"
            android:theme="@style/AppThemeForSensy"/>
        <activity
            android:name=".SenseyTouchActivity_"
            android:theme="@style/AppThemeForSensy"/>
    </application>

</manifest>